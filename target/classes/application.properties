# Nome da aplica��o (j� configurado)
spring.application.name=users

# Configura��o do banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/api-java
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Configura��o do JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# Configura��o do JPA para gerenciar o schema do banco de dados
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configura��o do pool de conex�es Hikari (padr�o do Spring Boot)
spring.datasource.hikari.maximum-pool-size=10
api.security.token.secret=${JTW_SECRET:my-secret-key}